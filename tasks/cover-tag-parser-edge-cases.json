{
  "id": "cover-tag-parser-edge-cases",
  "title": "Cover tag parser edge cases",
  "issue": "parseTags handles duplicate tags and empty comma-separated segments, but the public behavior is not protected by tests.",
  "repository": {
    "url": "https://github.com/nnennandukwe/pi-qodo-quality-fixture.git",
    "clone_ref": "main",
    "base_ref": "origin/main",
    "expected_base_sha": "2f37e9f3e1a5cb6d22e9f95e3c6c99e1772afa8f",
    "repo_full_name": "nnennandukwe/pi-qodo-quality-fixture"
  },
  "acceptance_criteria": [
    "Tests prove that empty and whitespace-only segments are ignored",
    "Tests prove that duplicate tags collapse while preserving first-seen order",
    "The existing comma-separated normalization behavior remains covered"
  ],
  "files_expected": [
    "tests/tags.test.ts"
  ],
  "files_allowed": [
    "tests/tags.test.ts"
  ],
  "required_test_files": [
    "tests/tags.test.ts"
  ],
  "constraints": [
    "Do not modify production code",
    "Use the existing node:test and strict assertion style"
  ],
  "non_goals": [
    "Do not change tag case sensitivity",
    "Do not add support for alternate delimiters"
  ],
  "verification": [
    {
      "id": "held-out-oracle",
      "argv": [
        "node",
        "--experimental-strip-types",
        "{harness_root}/oracles/tag-parser-edges.mjs",
        "."
      ],
      "required": true
    },
    {
      "id": "tests",
      "argv": ["npm", "test"],
      "required": true
    },
    {
      "id": "lint",
      "argv": ["npm", "run", "lint"],
      "required": true
    },
    {
      "id": "format",
      "argv": ["npm", "run", "format:check"],
      "required": true
    },
    {
      "id": "typecheck",
      "argv": ["npm", "run", "typecheck"],
      "required": true
    },
    {
      "id": "security",
      "argv": ["npm", "run", "security"],
      "required": true
    }
  ]
}
