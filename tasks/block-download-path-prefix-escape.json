{
  "id": "block-download-path-prefix-escape",
  "title": "Block download path prefix escapes",
  "issue": "resolveDownloadPath uses a string-prefix containment check, so a sibling directory whose name begins with the allowed root can be treated as inside that root.",
  "repository": {
    "url": "https://github.com/nnennandukwe/pi-qodo-quality-fixture.git",
    "clone_ref": "main",
    "base_ref": "origin/main",
    "expected_base_sha": "2f37e9f3e1a5cb6d22e9f95e3c6c99e1772afa8f",
    "repo_full_name": "nnennandukwe/pi-qodo-quality-fixture"
  },
  "acceptance_criteria": [
    "A sibling directory that shares the allowed root prefix is rejected",
    "Nested files inside the allowed root still resolve successfully",
    "Existing rejection of obvious traversal outside the root remains covered"
  ],
  "files_expected": [
    "src/files.ts",
    "tests/files.test.ts"
  ],
  "files_allowed": [
    "src/files.ts",
    "tests/files.test.ts"
  ],
  "required_test_files": [
    "tests/files.test.ts"
  ],
  "constraints": [
    "Use the existing null result for rejected paths",
    "Do not add filesystem access or dependencies"
  ],
  "non_goals": [
    "Do not resolve symbolic links",
    "Do not decode URL-encoded path input"
  ],
  "verification": [
    {
      "id": "held-out-oracle",
      "argv": [
        "node",
        "--experimental-strip-types",
        "{harness_root}/oracles/download-path-containment.mjs",
        "."
      ],
      "required": true
    },
    {
      "id": "tests",
      "argv": ["npm", "test"],
      "required": true
    },
    {
      "id": "lint",
      "argv": ["npm", "run", "lint"],
      "required": true
    },
    {
      "id": "format",
      "argv": ["npm", "run", "format:check"],
      "required": true
    },
    {
      "id": "typecheck",
      "argv": ["npm", "run", "typecheck"],
      "required": true
    },
    {
      "id": "security",
      "argv": ["npm", "run", "security"],
      "required": true
    }
  ]
}
