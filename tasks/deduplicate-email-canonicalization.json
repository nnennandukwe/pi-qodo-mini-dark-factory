{
  "id": "deduplicate-email-canonicalization",
  "title": "Deduplicate email canonicalization",
  "issue": "The login and recovery email exports duplicate the same trim-and-lowercase normalization logic, increasing the chance that their behavior drifts.",
  "repository": {
    "url": "https://github.com/nnennandukwe/pi-qodo-quality-fixture.git",
    "clone_ref": "main",
    "base_ref": "origin/main",
    "expected_base_sha": "2f37e9f3e1a5cb6d22e9f95e3c6c99e1772afa8f",
    "repo_full_name": "nnennandukwe/pi-qodo-quality-fixture"
  },
  "acceptance_criteria": [
    "Both existing exports preserve their current trim-and-lowercase behavior",
    "Both exports delegate to one shared local helper that implements the trim-and-lowercase normalization expression",
    "Regression tests cover both exported functions"
  ],
  "files_expected": [
    "src/identity.ts",
    "tests/identity.test.ts"
  ],
  "files_allowed": [
    "src/identity.ts",
    "tests/identity.test.ts"
  ],
  "required_test_files": [
    "tests/identity.test.ts"
  ],
  "constraints": [
    "Keep both exported function names and signatures",
    "Do not add or upgrade dependencies"
  ],
  "non_goals": [
    "Do not add email-address validation",
    "Do not change Unicode or locale behavior"
  ],
  "verification": [
    {
      "id": "held-out-oracle",
      "argv": [
        "node",
        "--experimental-strip-types",
        "{harness_root}/oracles/email-normalization-refactor.mjs",
        "."
      ],
      "required": true
    },
    {
      "id": "tests",
      "argv": ["npm", "test"],
      "required": true
    },
    {
      "id": "lint",
      "argv": ["npm", "run", "lint"],
      "required": true
    },
    {
      "id": "format",
      "argv": ["npm", "run", "format:check"],
      "required": true
    },
    {
      "id": "typecheck",
      "argv": ["npm", "run", "typecheck"],
      "required": true
    },
    {
      "id": "security",
      "argv": ["npm", "run", "security"],
      "required": true
    }
  ]
}
