{
  "id": "validate-service-port-range",
  "title": "Validate configured service ports",
  "issue": "parseServicePort rejects malformed values but accepts integers outside the valid TCP port range, including zero and values above 65535.",
  "repository": {
    "url": "https://github.com/nnennandukwe/pi-qodo-quality-fixture.git",
    "clone_ref": "main",
    "base_ref": "origin/main",
    "expected_base_sha": "2f37e9f3e1a5cb6d22e9f95e3c6c99e1772afa8f",
    "repo_full_name": "nnennandukwe/pi-qodo-quality-fixture"
  },
  "acceptance_criteria": [
    "Ports from 1 through 65535 are returned as numbers",
    "Zero, negative integers, and integers above 65535 return null",
    "Malformed and non-integer values continue to return null"
  ],
  "files_expected": [
    "src/config.ts",
    "tests/config.test.ts"
  ],
  "files_allowed": [
    "src/config.ts",
    "tests/config.test.ts"
  ],
  "required_test_files": [
    "tests/config.test.ts"
  ],
  "constraints": [
    "Do not change the function signature or return type",
    "Do not add or upgrade dependencies"
  ],
  "non_goals": [
    "Do not add environment-variable loading",
    "Do not redesign service configuration"
  ],
  "verification": [
    {
      "id": "held-out-oracle",
      "argv": [
        "node",
        "--experimental-strip-types",
        "{harness_root}/oracles/service-port-range.mjs",
        "."
      ],
      "required": true
    },
    {
      "id": "tests",
      "argv": ["npm", "test"],
      "required": true
    },
    {
      "id": "lint",
      "argv": ["npm", "run", "lint"],
      "required": true
    },
    {
      "id": "format",
      "argv": ["npm", "run", "format:check"],
      "required": true
    },
    {
      "id": "typecheck",
      "argv": ["npm", "run", "typecheck"],
      "required": true
    },
    {
      "id": "security",
      "argv": ["npm", "run", "security"],
      "required": true
    }
  ]
}
